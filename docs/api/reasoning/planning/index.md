# `reasoning/planning/index.ts`

============================================================ ST Planning — Barrel ============================================================ Planificación clásica STRIPS / PDDL light. API pública:   - `bfsPlan(problem, opts?)`       → BFS, plan de menor longitud   - `aStarPlan(problem, opts?)`     → A*, plan de menor costo   - `ground(action, bindings)`      → instanciar schema   - `applyAction(state, ground)`    → estado sucesor   - `goalSatisfied(state, goal)`    → goal ⊆ state   - `fastForwardHeuristic(...)`     → heurística FF por niveles   - `makeFFHeuristic(problem)`      → curry FF al problema   - `goalCountHeuristic`            → |goal\state| trivial Tipos: `STRIPSAction`, `STRIPSProblem`, `Plan`, `PlanStep`, `GroundedAction`, `Heuristic`, `PlannerOptions`, `AStarOptions`.
