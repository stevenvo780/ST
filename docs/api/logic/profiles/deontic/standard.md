# `logic/profiles/deontic/standard.ts`

============================================================ ST Deontic Standard — KD sobre BaseTableauProfile ============================================================ O(φ) = [](φ)  — obligación P(φ) = <>(φ)  — permisión F(φ) = [](!φ) — prohibición Axioma D: O(φ) → P(φ) — serialidad ============================================================

## `DeonticStandard`

> Class · `logic/profiles/deontic/standard.ts:15`

```ts
export class DeonticStandard extends BaseTableauProfile
```

