# `logic/profiles/substructural/index.ts`

============================================================ Substructural Logics — Public entrypoint ============================================================ Logicas substructurales (Linear y Affine) con calculo de secuentes intuicionistico unilateral. Conectivos multiplicativos (⊗, ⊸, 1), aditivos (&, ⊕) y el exponencial bang (!).   import {     LinearFormula,     proveLinear,     proveAffine,   } from 'src/profiles/substructural';
